import { COLOR } from "../../assets/constants/color";

const LoginScreen = () => {
  return (
    <div
      className={`w-screen h-screen flex items-center justify-center bg-[${COLOR.themeColor}]`}
    >
      <div className="w-1/4 h-10/12 bg-black">
      
      </div>
    </div>
  );
};

export default LoginScreen;
